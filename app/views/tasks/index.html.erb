<div id="wrapper">
  <%= render partial: "groups/side_user_bar", locals: { groups: @groups } %>
  <%= react_component("CalendarTask", 
    props = {tasks: @tasks, 
             group: @group, 
             user: current_user, 
             authenticity_token: form_authenticity_token},
    class: 'd-flex flex-column',
    id: 'content-wrapper') %>
</div>